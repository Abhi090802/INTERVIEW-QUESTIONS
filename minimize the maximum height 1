/*
  BRUTE FORCES APPROACH :RECURSION SE BHI KAR SAKTE HAI WITH EXPONENTIAL TIME COMPLEXITY 2^N AND SC=O(N) (ASC)
  AND  THEN DP CAN ALSO BE POSSIBLE BUT VERY COMPLICATED TO CODE4 SO IGNORE THAT APPRAOACH
  TC=O(NLOGN) SC=O(1)
*/
/*
  int getMinDiff(int arr[], int n, int k) {
    sort(arr,arr+n); int ans=arr[n-1]-arr[0];
    for(int i=1;i<n;i++){
        int mini=min(arr[i]-k,arr[0]+k);
        int maxi=max(arr[i-1]+k,arr[n-1]-k);
        ans=min(ans,maxi-mini);
    }
    return ans;
*/
